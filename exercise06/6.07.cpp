#include<bits/stdc++.h>
using namespace std;

int main() {

	/*

	fn preorder(node r){
	  static cnt = 0;
	  r.preorderId = cnt++;
	  preorder(r->left);
	  preorder(r->right);
	}

	fn inorder(node r){
	  static cnt = 0;
	  inorder(r->left);
	  r.inorderId = cnt++;
	  inorder(r->right);
	}

	fn postorder(node r){
	  static cnt = 0;
	  postorder(r->left);
	  postorder(r->right);
	  r.postorderId = cnt++;
	}
	*/
}
